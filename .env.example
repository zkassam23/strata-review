# Copy to .env. Only needed for real runs; tests and --mock never read it.
ANTHROPIC_API_KEY=sk-ant-...
# Optional overrides
# STRATA_LLM=mock          # force the offline heuristic client
# STRATA_TENANT=config/tenant.yaml
# STRATA_TAXONOMY=config/taxonomy.yaml
